# 遇到的问题(记录 & 复盘)

## 1. CMake 太新 vs 老项目
- 现象:`cmake_minimum_required(VERSION 3.1)` 报 "Compatibility with CMake < 3.5 has been removed"
- 解决:配置时加 `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`

## 2. samples 的 TARGET_BOARD
- 现象:误用 rk3588 会定义 CYPERSTEREO_RK3588 + 核绑定,不适合 Jetson
- 解决:TARGET_BOARD=auto(设备端自动 `-mcpu=native`)

## 3. Dockerfile 里 heredoc RUN 不可靠
- 现象:`docker history` 显示 RUN 只有首行 `/bin/sh -c python3 - <<'EOF'`,body 被静默丢弃
- 解决:脚本 COPY 进镜像再 RUN

## 4. cv_bridge 在 arm64 的加载顺序坑(最折腾的一个)
- 现象:`SystemError: initialization of cv_bridge_boost raised unreported exception`
- 原因:cv_bridge_boost 的模块 init 内部 `PyImport_ImportModule("cv2")`,若 cv2 没被先 import 则静默失败
- 解决:① apt ros-noetic-cv-bridge 二进制在 arm64 崩溃 → 从 vision_opencv(noetic 分支)源码编译 cv_bridge;② `patch_cvbridge.py` 在每个 `import cv_bridge` 前插入 `import cv2`
- 注:sitecustomize 预加载 cv2 的方案无效(启动时 sys.modules 里仍没有)

## 5. kalibr 顶层 python 需要 wx
- 现象:imu 工具 `from sm import PlotCollection ... import wx` 报错
- 解决:apt 装 `python3-wxgtk4.0`

## 6. data_cap Ctrl+C 退不出
- 现象:采集线程忽略 SIGINT 无限重试
- 解决:`kill -9`;但 `pkill -9 -f capture_image_imu` 会匹配到命令行本身 → 用精确 PID

## 7. 相机枚举 / 序列号
- 现象:最初 lsusb 无相机、/dev/video* 空、"no frame retrying"
- 结果:后枚举成功;PID 一度是 04b4:**00f8**(SDK 预期 00f9),恢复后为 04b4:**00f9**
- 序列号:sysfs `serial=m000023` → 官方出厂标定文件 `cyperstereo_sn_m023.yaml`

## 8. rosbags msgtype 显示带 /msg/ 不是问题
- 现象:rosbags 读出的 type 是 `sensor_msgs/msg/Imu`(内部规范名)
- 验证:以容器里 ROS1 原生 `rosbag info` 为准,实际写盘是 `sensor_msgs/Imu`

## 9. rosbag1 Writer 细节
- `add_connection()` 除 topic/msgtype 外全 keyword-only
- reader 的 `msgdef` 是封装对象,写时要取 `.data`
- 目标文件已存在会拒写(不会覆盖),先 `rm`

## 10. 抽帧脚本把 IMU 全丢了
- 现象:kalibr 报 "Could not find topic /imu0"
- 原因:`count % 1 == 1` 恒假(step=1 时一条都不留)
- 解决:`if step == 1 or count % step == 1`
- 现成脚本:kalibr_ws/scripts/subsample_bag.py

## 11. 畸变模型选错
- 现象:用 `pinhole-radtan` 跑(慢且不合适)
- 原因:M60 是鱼眼,KannalaBrandt8
- 解决:`--models pinhole-equi pinhole-equi`

## 12. 全量 4789 帧标定太慢
- 现象:kalibr 角点提取卡在 4789/4789(实际是慢不是死)
- 解决:抽帧(camcalib 599 帧/目、imucam 1597 帧/目 + 全量 IMU)

## 13. 报告 PDF 生成失败不影响结果
- 现象:双目标定结尾报 `igraph ... plotting not available`
- 原因:缺 cairo 绘图
- 结论:camchain / results txt 在报错前已写盘;imu-cam 用的是 matplotlib,能正常出 pdf

## 14. 官方 yaml 多个 SN 别串
- m023 / m024 Tbc 不同;引数值前先 cat 目标文件确认
- kalibr 与官方 Tbc 的 z 反号:大概率是 yaml 存逆矩阵(Tbc vs Tcb)的约定差异,不是标错

## 15. 后台跑 ros2 bag play
- 后台任务 stdin 非 tty,键盘控制(空格暂停等)不可用;要看进度直接前台跑

## 16. 无头环境跑 Pangolin 直接 abort
- 现象:ORB-SLAM3 online 一启动就 `terminate: Pangolin X11: Failed to open X display`(核心转储)
- 原因:SSH/无桌面,没有 X DISPLAY,Pangolin Viewer 起不来
- 解决:① 本机有 X server(如接显示器/本地桌面)就 `export DISPLAY=:N` 连上;② 真没 X 就
  `xvfb-run -a -s "-screen 0 1280x800x24" <exe> ...`(虚拟屏,窗口看不见但 SLAM 照跑)

## 17. 本机 X server 在 :1 不在 :0
- 现象:`DISPLAY=:0` 但 `xdpyinfo` 报 unable to open display ":0"
- 原因:常见 .bashrc 自动设 DISPLAY 是 `who` 抓不到就默认 `:0`,而本机只有 `/tmp/.X11-unix/X1`(:1)
- 解决:扫实际 socket `DISPLAY=":$(ls /tmp/.X11-unix/ | grep -o '[0-9]*' | head -1)"`,别写死 :0;
  另需 `xhost +SI:localuser:$USER`(针对对应 DISPLAY)放行本地客户端
