# 真值评估算法(动捕 GT vs SLAM 轨迹)

> 目标:离线对齐时空外参后,评估 SLAM 轨迹相对光学动捕真值的 **APE / ARE**。
> 数据样本:`compara/`(README 见 [compara/README.md](compara/README.md))。
> 实现:**evo(行业标准)引擎** —— 指标与对齐交给 evo(`evaluation/.venv`),脚本只做
> 读取(GT vicon csv / est 8 列 txt)→ 造 evo 轨迹 → **自动扫时间偏移 Δ** → evo 关联/对齐/算误差 → 出 5 项交付物。

## 1. 输入与配对

| 相机 | 动捕真值 GT | SLAM 估计 est |
|---|---|---|
| 150(头/ego) | `hes3/hes3-ego.csv` | `slam_time_aligned/CameraTrajectory_150_time_aligned.txt` |
| 151(左手) | `hes3/hes3-left_hand.csv` | `.../CameraTrajectory_151_time_aligned.txt` |
| 152(右手) | `hes3/hes3-right_hand.csv` | `.../CameraTrajectory_152_time_aligned.txt` |

- GT:Vicon 层级 csv,取列 `Timestamp`(unix **ms**)、`X Y Z`(**mm**)、`Qx Qy Qz Qw`。
- est:取列 `session_time`(录制起点 0 起的**秒**)、`tx ty tz`、`qx qy qz qw`(米制量级)。

## 2. 数据清洗与时间轴

1. **GT 修时间戳**:时间戳 ≤0 或非单调的帧按帧序号用邻近有效帧线性补时间;四元数全零(姿态无效)帧剔除。
2. 统一单位:**GT mm→m**。
3. 时间基准:**GT epoch 秒 → session 秒**(compara 减 `record_start_target_unix`;自定义 GT 默认减其首帧)。
4. **自动扫时间偏移 Δ**:est 上位机与动捕机未对时(compara 实测差 ~**+1.8s**),扫 `est+Δ≈gt` 使
   evo 关联后平移 rmse 最小的 Δ(默认搜索 0~4s,步长 0.05s,谷底再细扫)。这是两机时钟差,不是固定参数。
5. **evo 关联**:`associate_trajectories(ref, est, max_diff=0.02, offset_2=Δ)`,90Hz GT 足够密,est 逐点取到配对,只取公共窗。

## 3. 空间对齐(evo Umeyama)

- evo `traj_est.align(traj_ref, correct_scale=False)`:**SE(3)**(刚性,scale=1)主口径;`correct_scale=True` 估 **Sim3** 作 sanity(看尺度 s 是否≈1)。
- 对齐后 est 全体位姿(平移+旋转)进入动捕全局系,与 GT 逐点比。

> ⚠️ 旋转/ARE 提示:evo 的 `align` 只按**位置**做 Umeyama。若 est 机体坐标系与动捕刚体坐标系存在**静态轴/安装旋转差**(compara 实测 ~100°),
> ARE 会整体虚高但 std 极小(≈常数)。要得到可比的 ARE,须先标定并扣掉该静态旋转(蘅芜君 Ego 表旋转 rmse 0.94° 即来自坐标约定一致的 est)。

## 4. 指标(每个公共时刻点)

- `APE_i` = ‖gt_pos_i − est'_pos_i‖ → 记 **mm**。
- `ARE_i` = gt 与 est' 姿态间旋转角 → 记 **deg**(acos 法)。
- 统计(APE、ARE 各一套):**max / mean / median / min / RMSE / SSE / std**
  - mean=Σe/N, RMSE=√(Σe²/N), SSE=Σe², std=√(Σ(e−mean)²/N)

## 5. 输出物(5 项)

### ① 汇总表
每台相机两行(APE/mm、ARE/deg),各列 max/mean/median/min/RMSE/SSE/std,另附 Sim3 尺度 s、时间偏移 Δ、样本数 N、公共窗。
写 `results/summary.json`。

### ② APE 时序曲线
x=session_time,y=APE(mm)。叠加统计线:**mean/median/RMSE** 虚线、**min/max** 参考,**±1σ 阴影带**;
SSE 与其余统计写入图注/图例(SSE 是标量,不入曲线)。每台一张,三台并排或分页。

### ③ 3D 轨迹热力图
3D 空间画 GT 轨迹线,按该处 APE 上色(colormap),标注起终点,colorbar 单位 mm;
est(对齐后)可淡色叠加验证贴合度。**颜色越红=误差越大,能直观看出轨迹哪一段差。**

### ④ XYZ 位置对比曲线
X/Y/Z 三个子图:x=session_time,y=位置(m),GT 与 est' 各一条——**两线贴合=时间同步对**,
分离=时基没对上。单位 m。

### ⑤ RPY 欧拉角曲线
Roll/Pitch/Yaw 三个子图:x=session_time,y=角度(deg),GT 与 est' 各一条——**看旋转是否跟得上、有没有丢转**。
四元数 → 欧拉,约定 XYZ 内旋(如与应用不同可调)。

## 6. 运行与产物

```sh
evaluation/.venv/bin/python evaluation/evaluate.py            # 默认吃 compara 三对
evaluation/.venv/bin/python evaluation/evaluate.py --unit 150 # 只跑一台
# 可选:--est x.txt --gt y.csv --name my     喂任意自己的轨迹(est:8 列会话秒 txt;gt:Vicon csv)
#      --lo/--hi/--step 改偏移搜索范围;--auto-offset 1.8 跳过搜索直接用
```
- 依赖:evo + numpy + matplotlib(在 `evaluation/.venv`)。
- 产物:`results/summary.json` + 每台子目录 4 张 PNG(APE 时序 / 3D 热力图 / XYZ / RPY)。
- est 时间轴须与 GT 同零点(会话秒);原始 `CameraTrajectory.txt`(相机时钟 ~1679)须先经 `timestamps.csv`
  换算成会话秒(见 [compara/数据分析.md](compara/数据分析.md)),`slam_time_aligned/` 即该产物。
